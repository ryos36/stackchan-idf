# Core2 移植の変更履歴

Core2 (M5Stack Core2 / ESP32 無印) 対応の作業 (`72f34cb`〜) で入った
変更の記録。git のコミット メッセージとは別に、あとから経緯を追える
ようにまとめる。

| コミット | 内容 |
|---|---|
| `72f34cb` | Core2 (ESP32無印) 対応を追加 |
| `8a089d7` | タスクスタックの内部RAM化を共通ヘルパーに一元化 |
| `0a1374c` | esp-sr を 2.4.0 に固定 (2.5.0 の esp-dl 依存が CoreS3 のパーティションを溢れさせるビルド不具合への対応) |
| `2d61200` | Gemini Live のツール引数が渡らない問題を修正 (`parametersJsonSchema` → `parameters` + 大文字化) |
| `0723e2e` | Gemini Live 応答の吹き出しが表示されない問題を修正 (`AssistantTextDone` 未発行) |
| `37d7bd2` | monitor_log.py がダウンロードモードに落ちる問題を修正 (開発ツール側の不具合) |
| `3b19edd` | base64 デコードを表引きに置き換え (mbedtls の2パス実装 → 256要素の変換表による1パス実装) |
| `91cd9e9` | `assistant_pcm_` が20秒ぶんの予約容量を超えたら、PSRAM再確保をせず以降を捨てるガードを追加 |
| `643df6d` | TODO.md の記述訂正 (Core2 の HMM 非対応理由は容量制約ではなくパーティション表の設計判断) |

`2d61200` と `0723e2e` は、Gemini Live 対応そのものに存在した不具合の
修正 (オリジナルの実装が抱えていたバグ)。`0a1374c` と `37d7bd2` は
実験やチューニングではなく、ビルド・開発ツール側の不具合修正。
